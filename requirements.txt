# Core ML Libraries
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# Deep Learning
torch>=2.0.0
transformers>=4.30.0

# Optional but recommended for GPU support
# torchaudio>=2.0.0 (uncomment if using audio features)
# torchvision>=0.15.0 (uncomment if using vision features)

# Note: Install PyTorch with CUDA support if you have an NVIDIA GPU
# Visit https://pytorch.org/get-started/locally/ for installation instructions
